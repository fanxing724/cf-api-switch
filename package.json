{
  "name": "cf-api-switch",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker 多渠道协议转换网关：把只认老 chat/completions 的站点（DeepSeek / 火山方舟 / NewAPI 中转）统一转成 OpenAI 新 Responses 协议对外，带 KV 存储与管理面板。",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=18"
  }
}
